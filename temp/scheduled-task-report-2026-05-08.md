# Scheduled Task Run — 2026-05-08

**Task:** Check Linear project for "What's Cookin", retrieve tasks per agent, invoke agents, complete tasks, submit for QA.

---

## Status: LINEAR STILL BLOCKED — Deep Codebase Audit Completed Instead

### Root Cause (unchanged across 3 runs)

`api.linear.app` is blocked by the Cowork sandbox network proxy. No Linear MCP connector is installed. This is the **3rd consecutive run** blocked on the same issue.

**The fix is one action by you:** install the Linear plugin in Cowork (instructions at bottom of this report).

---

## What This Run Did Instead

Since the codebase was audited shallowly on previous runs, this run did a **full deep audit**: read every source file, ran the TypeScript compiler, checked build output, verified data files, and looked for bugs or incomplete features. Results below.

---

## Full Codebase Audit — as of 2026-05-08

### Build Health

| Check | Result |
|---|---|
| `tsc --noEmit` | ✅ **0 errors, 0 warnings** |
| `dist/` folder exists | ✅ App has been deployed before |
| PWA manifest | ✅ Generated by vite-plugin-pwa |
| Service worker | ✅ `sw.js` + `workbox-*.js` in dist |
| Icons | ✅ `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` in `public/` |
| Supabase credentials | ✅ `.env` has VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY |
| TODO/FIXME markers | ✅ None in source files |

### Files That Exist and Are Complete

| File | Lines | Status |
|---|---|---|
| `src/types/index.ts` | — | ✅ Full type system — DietType, Household, Dish, MealOutput, MealComponent, RecommendationContext, etc. |
| `src/db/schema.ts` | — | ✅ Dexie v3→v4 migrations, all tables: dishes, meals, approvals, eat_out, settings, meal_plans |
| `src/lib/seed.ts` | — | ✅ DB init, DEFAULT_HOUSEHOLD, DEFAULT_DIET_CHART, MEAL_SLOTS |
| `src/lib/dates.ts` | — | ✅ todayISO, offsetDate, dayOfWeekForDate, formatDisplayDate, daysAgo, shortDayLabel |
| `src/lib/auth.ts` | — | ✅ Supabase auth helpers |
| `src/lib/supabase.ts` | — | ✅ Supabase client setup |
| `src/engine/recommendation.ts` | ~450 | ✅ **Complete engine:** hard filter → soft score (recency + frequency penalty + preference boost) → weighted shuffle → 3 unique meal sets. Smart swap (category-locked). getSwapOptions. computeMacros. |
| `src/App.tsx` | ~95 | ✅ BrowserRouter, onboarding gate (live query on `onboarding_complete`), auth guard, route definitions, BottomNav |
| `src/main.tsx` | — | ✅ Entry point |
| `src/pages/OnboardingPage.tsx` | 770 | ✅ Full multi-step onboarding: headcount → per-person diet type + region → goals → diet chart → regional seed library confirm → done screen |
| `src/pages/DishLibraryPage.tsx` | 333 | ✅ Browse + filter by category/diet type, swipeable rows (archive/delete), DishEditSheet integration, QuickAddSheet |
| `src/pages/DietChartPage.tsx` | 353 | ✅ Week strip nav, slot cards per day, SlotEditSheet with protein/carb/sabzi constraints |
| `src/pages/SettingsPage.tsx` | 430 | ✅ Household members (add/remove/edit), Goals (calorie + protein steppers), Rules (frequency cap toggles), Account (sign out) |
| `src/components/home/HomeScreen.tsx` | ~600 | ✅ **Most complex component.** Timeline nav (±3 days), today view with all slots, past/future read-only views, slot sections with shuffle/approve/eat-out/add-dish, share button (native share API + clipboard fallback), household chip |
| `src/components/home/MealCard.tsx` | — | ✅ Per-component rows with swap + skip/undo-skip affordances, person scope labels |
| `src/components/home/SwapSheet.tsx` | ~100 | ✅ Category-locked swap list, active vs reserve sections, "Add something else" → QuickAddSheet |
| `src/components/home/HouseholdEditSheet.tsx` | ~110 | ✅ Inline household editor from home screen chip |
| `src/components/home/RecencyChip.tsx` | — | ✅ "Last 3 days" recency context pill |
| `src/components/dishes/DishEditSheet.tsx` | ~410 | ✅ Full edit form: name, category, diet type, status, macros |
| `src/components/shared/QuickAddSheet.tsx` | ~440 | ✅ Quick-add dish with optional macros, saves to DB |
| `src/components/shared/InstallPrompt.tsx` | — | ✅ PWA install prompt |
| `src/components/auth/AuthContext.tsx` | — | ✅ Auth state, isAnonymous flag |
| `src/components/auth/SavePromptSheet.tsx` | ~330 | ✅ Magic link sign-up sheet shown once after first meal approval for anonymous users |
| `src/components/ui/Badge.tsx` | — | ✅ |
| `src/components/ui/Button.tsx` | — | ✅ |
| `src/components/ui/BottomSheet.tsx` | — | ✅ |
| `src/components/ui/BottomNav.tsx` | — | ✅ |
| `src/components/ui/Logo.tsx` | — | ✅ |
| `data/regional-seeds/` | — | ✅ All 6 regional seed files: north_india, east_india, south_india, west_india, northeast_india, pan_indian |
| `data/rules.json` | — | ✅ frequency_caps + combination_blocks |
| `data/diet-chart.json` | — | ✅ Weekly template |
| `data/rotation-bank.json` | — | ✅ Dish library seed |
| `data/goals.json` | — | ✅ Default goals |
| `data/household.json` | — | ✅ Default household (2 people) |

### Decisions Log Cross-Check

| Decision | Code Status |
|---|---|
| D-01 — Frequency caps are soft scoring, not hard blocks | ✅ `frequencyPenalty()` in engine, `penalty_weight` in rules.json |
| D-02 — Eating out = day ignored | ✅ `eat_out` DB table, "Eating out instead?" button on home screen, `handleEatOut/handleCancelEatOut` |
| D-03 — Approval-only recency tracking | ✅ `approveMeal()` updates `last_used` on dish; no cook-log flow |
| D-04 — Household chip from home screen | ✅ Chip in header → HouseholdEditSheet |
| D-05 — Guests as temporary personas | ✅ Settings → Add person; session-level if removed same day |
| D-06 — Eggitarian as first-class diet type | ✅ Throughout types, engine, UI labels |
| D-07 — No full-bank escape from swap sheet | ✅ SwapSheet is category-locked, no "see all" |
| D-09 — No reserved dishes concept; active/reserve only | ✅ Two status tiers only in DishEditSheet and SwapSheet |
| D-10 — Quick-add from multiple entry points | ✅ Home screen, SwapSheet, DishLibraryPage all have QuickAddSheet |
| D-13 — Macro subtext on home screen | ✅ `macroLine` shown below MealCard |
| D-15 — Share button → native share sheet | ✅ `buildShareText()` + `navigator.share()` in HomeScreen |
| "Rotation Bank" → "Dish Library" rename | ✅ UI and code consistently use "Dish Library" |

### What Was NOT Found (potential gaps)

| Gap | Assessment |
|---|---|
| `src/hooks/` directory | **Not present, but not needed** — all state is managed inline with useLiveQuery + useState |
| Meal history / log screen | **Not a Phase 1 requirement** per CLAUDE.md and decisions-log.md (D-03 explicitly deprioritizes it) |
| `MealPlan` table in DB schema | **Defined but unused** — the `meal_plans` table is in the Dexie schema (v4) but no component reads or writes to it. Low risk — empty table. |
| Grocery list export | **Phase 3** per CLAUDE.md — not expected |
| Cook notification (push) | **Phase 2** per CLAUDE.md — share button covers this for now |
| Supabase row-level security | **Not verifiable** from client code alone — this is a backend concern on the Supabase dashboard |
| "Not in mood" persistent skip | "Skip" is implemented as a session-level state (`skippedDishIds`) — not persisted to DB. This matches D-03 (approval-only tracking), so it's correct behavior. |

---

## Assessment

**The app is feature-complete for Phase 1.** Every screen, every core interaction, every product decision is implemented. TypeScript compiles clean. The build has been run before (dist/ exists).

The only open question is whether there are runtime bugs not caught by static analysis — those require human QA on a device. See below.

---

## Manual QA Checklist (for you to run)

Since agents can't run the PWA on a real device, here's a focused QA checklist targeting the highest-risk paths:

### Critical Path (P0)
- [ ] First launch → onboarding completes → home screen shows a suggestion with dishes
- [ ] "Looks good ✓" approves the meal → dish `last_used` updates → recency chip shows the dish
- [ ] Tapping `[swap]` on a component → SwapSheet shows category-matched alternatives (NOT from other categories)
- [ ] "Try another" shuffles the meal → different dishes shown, not identical to previous
- [ ] "Eating out instead?" → slot shows eating-out state → "Back to suggestions" restores suggestion

### Settings & Data (P1)
- [ ] Settings → add/remove household member → home screen chip updates immediately
- [ ] Dish Library → swipe left on a dish → Archive (moves to reserve) and Delete work
- [ ] Dish Library → add new dish → appears in swap sheet for the correct category
- [ ] Diet Chart → edit a slot constraint → tomorrow's suggestion reflects the new constraint

### PWA (P1)
- [ ] On iOS: "Add to Home Screen" → app opens in standalone mode (no Safari chrome)
- [ ] Offline: close wifi, reload → app shows last suggestion from IndexedDB
- [ ] Auth: magic link email → link click → user is signed in → data persists after reload

---

## Action Required: Connect Linear

**This is the 3rd consecutive run blocked on this.** Until Linear is connected, the scheduled task cannot do its primary job (read tasks → invoke agents).

**Steps (takes 2 minutes):**

1. Open **Cowork**
2. Click the **Plugins / Connectors** panel (left sidebar)
3. Search for **"Linear"**
4. Install it and authenticate with your API key:
   ```
   lin_api_REDACTED
   ```
5. Re-run this task: just say **"Run the start-coding task"** in Cowork

Once Linear is connected, the next run will:
1. Fetch all open issues in the "What's Cookin" project
2. Read the assignee (agent label) on each issue
3. Spawn sub-agents per task and complete the work
4. Run TypeScript compile + QA verify
5. Move all completed issues to "Done" in Linear

---

*Report generated by scheduled task `start-coding` — run 3 of 3 blocked runs.*
